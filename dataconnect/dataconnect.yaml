specVersion: "v1beta"
serviceId: "dotapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dotapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
